# Web

React + Vite frontend for the monitoring and orchestration dashboard.

## Run locally

1. Install dependencies with `npm install`
2. Start the dev server with `npm run dev`

## Current scope

- lightweight dashboard shell
- language selector placeholder
- placeholder cards for system coverage, removable disks, and backup history
