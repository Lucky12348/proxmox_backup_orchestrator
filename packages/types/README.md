# Shared Types

Shared schemas and contracts for cross-component communication will live here.

Potential future content:

- JSON schemas
- OpenAPI-derived contracts
- shared TypeScript types
- Python-compatible contract definitions
