# CLAUDE.md

This project's AI agent guide is tool-agnostic and lives in `AGENTS.md` so it
stays usable by Claude Code, Codex, Cursor, and any other agent.

@AGENTS.md

Everything in `AGENTS.md` applies here, including the hard rule: never run
`git commit` or `git push` on your own initiative — leave changes uncommitted
and let the user decide when to commit/push.
